# Infinite Canvas 操作手册

## 调整界面字体

系统首次运行时会根据设备可用宽度、物理分辨率和屏幕宽高比选择推荐字号，并对 34 英寸常见超宽屏及 4K 显示器使用更大的推荐值。界面使用接近 Infinite Canvas 的可读字号层级，辅助文字不再使用低于约 11px 的微小字号。右上角的“A− / 百分比 / A＋”用于缩小或放大整个界面的文字，范围为 90%—200%；点击中间的百分比可恢复当前设备的推荐字号。用户选择会保存在当前设备，下次打开时继续使用。

按钮、输入框、选择框、文本域、节点连线端口和四角缩放手柄都会随字号同步增长，因此在超宽屏和 4K 显示器上不仅文字更大，所有控件的点击与拖拽区域也会相应增大。端口和缩放手柄始终以原中心点向外扩展，不会改变连线锚点和节点边界位置。

本文档记录当前网页中已经实现的画布、节点、连线、AI 生成和配置功能，便于日常使用与后续维护。文档以当前代码为准；尚未接入或界面选项暂未生效的能力会在“当前限制”中明确说明。

## 目录

- [快速开始](#快速开始)
- [画布与视图](#画布与视图)
- [节点通用操作](#节点通用操作)
- [有向连线与上游输入](#有向连线与上游输入)
- [文本节点](#文本节点)
- [图片节点](#图片节点)
- [视频节点](#视频节点)
- [音频节点](#音频节点)
- [AI 生成规则](#ai-生成规则)
- [配置中心](#配置中心)
- [保存、导出与快捷键](#保存导出与快捷键)
- [推荐流程](#推荐流程)
- [当前限制](#当前限制)

## 快速开始

1. 从左侧工具栏新建文本、图片、视频或音频节点，也可以点击“上传”选择本地素材。
2. 在文本节点中填写任务或提示词。
3. 从上游节点右侧输出端口连到下游节点，箭头方向就是数据传递方向。
4. 选中下游节点，确认节点底部显示的输入数量。
5. 点击下游节点的“生成”按钮。系统只会读取直接连接且已启用的上游节点。
6. 如需调用真实模型，请先在右上角“配置”中填写对应节点类型的 API Base URL、API Key 和模型名称。

未填写 API Key 时，点击“生成”只会返回一段本地演示文字，不会请求模型，也不会生成真实图片。

## 画布与视图

### 移动画布

在画布空白处按住鼠标左键或中键拖动，可以向任意方向平移画布。画布位置不受固定边界限制，右下角导航会同步显示当前视口和节点位置。

左侧的箭头按钮对应选择模式，手掌按钮对应移动画布模式；也可以按 `V` 和 `H` 切换。当前版本在空白区域拖动时，两种模式都可以平移画布。

### 缩放画布

鼠标位于画布区域时滚动滚轮，可以以鼠标位置为中心缩放，范围为 35%–200%。

右下角提供以下视图按钮：

- `−`：缩小画布。
- 百分比：显示当前缩放比例，点击后恢复 100% 并回到默认视口位置。
- `＋`：放大画布。
- 房屋图标：重置视图。
- 导航图标：显示或隐藏右下角小地图。
- 删除图标：确认后清空当前画布的全部节点和连线。

当鼠标位于文本输入区、AI 结果区、图片设置面板或表单控件上时，滚轮用于滚动其中的内容，不会缩放画布。

### 背景样式

在“配置 → 画布”中可以把背景切换为“点阵”或“网格”。

## 节点通用操作

### 新建节点

左侧工具栏可以新建文本、图片、视频和音频节点。新节点会创建在当前视口附近，并自动成为选中节点。

“上传”支持选择图片、视频或音频文件。网页会读取本地文件并创建相应类型的节点，节点标题使用原始文件名，素材保存在当前浏览器中的画布数据里。

### 移动节点

按住节点中不属于按钮、输入框、媒体控件或尺寸手柄的区域拖动，可以移动整个节点。图片内容与图片节点一起移动，松开鼠标即结束拖动。

### 调整节点大小

选中或悬停节点后，从节点四个角的尺寸手柄拖动，可以同时修改宽度和高度。节点最小宽度为 220，最小高度为 160。

图片预览会保持图片节点设置的画面比例；调整节点外框时不会把生成说明区域挤成主要显示区域。

### 修改标题

节点顶部标题是可编辑输入框。修改标题后，节点版本会增加，使用该节点作为输入的下游结果会被判定为可能过期。

### 选择与删除

点击节点会选中它，画布上方会出现当前节点操作栏。

- 非图片节点提供“连接”和“删除”。
- 图片节点提供“放大”“下载”“替换图片”“反推提示词”和“删除”。
- 也可以选中节点后按 `Delete` 或 `Backspace` 删除。

删除节点时，与该节点相连的所有连线也会一并删除。

## 有向连线与上游输入

### 连线的含义

连线不是装饰，而是有方向的数据依赖。`A → B` 表示 A 是上游、B 是下游；执行 B 时，A 的有效内容会进入 B 的生成上下文，B 不会反向影响 A。

一个节点可以连接多个下游节点，一个下游节点也可以接收多个上游输入。

### 创建连线

有两种连接方式：

1. 从上游节点右侧圆形输出端口按住并拖动，松开到目标节点任意区域。
2. 选中非图片节点后点击浮动操作栏中的“连接”，再点击下游节点。也可以直接点击节点左侧输入端口完成第二步。

目标位置不需要严格对准左侧输入端口。拖到目标节点卡片的任意区域即可识别。

系统会阻止以下连接：

- 节点连接自身。
- 相同方向的重复连线。
- 会形成循环依赖的连线。
- 指向已经不存在的节点的连线。

### 管理连线

点击连线后会出现连线操作栏，其中显示“上游节点 → 下游节点”和当前输入顺序。

- `↑` / `↓`：调整该连线在下游节点输入中的顺序。
- “停用”：临时停止这条连线的数据传递，但保留连线。
- “启用”：重新让该上游参与生成。
- “删除”：移除连线。移除后，该上游会立即停止参与下游生成。

输入首先按连线顺序排列；如果顺序相同，再按上游节点在画布中的纵向位置排列。

## 文本节点

### 输入文本

新建文本节点的内容区默认是空的。“描述你的创意想法，或粘贴一段需要处理的文本…”只是灰色占位提示，不会作为实际内容发送，也不需要手动删除。

直接点击内容区即可输入和修改文字。输入区可以独立滚动。

### 使用 `@` 引用已连接资源

文本输入区支持引用当前节点已经连接的直接上游文本、图片、视频和音频资源：

1. 先把资源节点连接到当前文本节点。
2. 在输入区键入 `@`。
3. 从弹出的资源列表中点击资源，或用上下方向键选择后按回车。
4. 引用会作为不可拆开的标签插入文本。

图片引用会显示真实缩略图。其他资源显示类型图标。每种资源会根据当前有效上游顺序自动编号，例如“图片 1”“文本 1”，不需要用户记忆或手写编号。

资源标题超过 10 个字符时，引用标签和候选列表只显示前 10 个字符，后面使用 `...`。鼠标悬停可以查看完整标题；内部仍保存节点 ID，发送给模型时也不会因为界面截断而丢失完整节点关系。

只有已经连接到当前文本节点的资源会出现在 `@` 菜单中。删除或停用连线后，该资源不再属于当前有效上游；已经插入但找不到资源的引用会显示为“已断开的资源”。

### 生成提示词或文字结果

在文本节点中输入需求后点击“生成”。系统会加入一个不显示在输入区中的默认任务：

> 请根据用户输入内容反推一段适合用于 AI 生图的提示词，并只输出可直接用于生图模型的提示词正文。

实际请求还会包含全局系统提示词、当前画布提示词和所有直接上游输入。生成结果显示在当前文本节点下半部分。

### 编辑 AI 生成结果

双击 AI 生成的文字进入编辑状态。编辑完成后可点击节点外部，或按 `Ctrl+Enter` / `Cmd+Enter` 保存修改。

输入区和 AI 结果之间有一条分割线。按住分割线上下拖动，可以调整两部分的高度。编辑区和结果区中的滚轮只滚动文字，不缩放画布。

### 用文本创建生图流程

点击文本节点右上角“生图”，系统会在右侧创建一个图片节点，并建立“文本节点 → 图片节点”的连线。

这一步只创建流程，不会自动生成图片。请检查新图片节点的比例设置，然后手动点击图片节点的“生成”。

## 图片节点

### 添加或替换图片

可以通过左侧“上传”直接创建本地图片节点。也可以先创建图片节点，再选中它并点击浮动操作栏中的“替换图片”。

替换完成后：

- 节点标题会改成上传文件的完整文件名。
- 节点内容会记录文件名、大小和“本地资产”。
- 节点版本会增加，下游已有结果会提示可能过期。

### 设置画面比例

点击图片节点右上角齿轮打开尺寸设置。

可选预设包括：

- 自动，且为新图片节点的默认值。
- 1:1。
- 3:2。
- 2:3。
- 16:9。
- 9:16。

也可以手动填写 W 和 H。输入值会被限制在 64–4096 之间。生成时，系统会把目标宽高和比例写入提示词，并选择模型接口支持的最接近输出尺寸。

“自动”模式下，生成提示会要求模型根据内容选择横向、竖向或方形构图；加载已有图片时，节点会读取图片原始宽高用于完整比例展示。

“生成张数”可以设置为 1–8，默认值为 1。该数值表示本次生成的总图片数量。

### 生成图片

将文本节点连接到图片节点，可提供生图提示词；将已有图片节点连接到图片节点，可提供视觉参考。点击图片节点“生成”后：

- 没有上游参考图片时，请求图片服务的 `POST /images/generations`。
- 有一个或多个上游参考图片时，请求 multipart `POST /images/edits`，参考图片以 `image[]` 上传。
- 如果当前图片节点本身已有图片，该图片也会作为参考图加入整批请求；批次开始后会固定使用原图，不会因为第一张新结果回填而改变后续请求。
- 只使用直接、已启用的上游节点。
- 生成张数为 1 时，返回的图片 URL 或 Base64 图片会写回当前图片节点。
- 生成张数大于 1 时，第一张写回当前主图节点，其余 `生成张数 - 1` 张创建为独立图片结果节点，并纵向排列在主图右侧。
- 整批图片全部请求成功后才会写入画布，避免只创建半批结果。新建的子图节点不会自动建立连线，因为它们与主图属于同一批结果，而不是把新主图作为生成输入。

系统不会因为创建图片节点、点击“生图”或建立连线而自动发起生成。

### 图片操作

选中图片节点后可使用：

- “放大”：打开大图查看层。
- “下载”：保存当前图片，文件名格式为 `YYYY-MM-DD_HH-MM-SS.png`。
- “替换图片”：从本地选择新的图片。
- “反推提示词”：在图片右侧创建文本节点，并自动建立“图片 → 文本”的连线。

大图查看层也提供下载、替换和反推提示词。

### 单击修改生成图片

单击由模型生成的结果图片，会在该图片节点下方打开“修改图片”控件。本地上传且尚未经过模型生成的图片不会打开该控件。

修改控件会显示这张图片生成时实际提交给图片接口的提示词。用户可以直接修改文字，并独立设置：

- 自动、1:1、3:2、2:3、16:9 或 9:16 画面比例。
- 64–4096 范围内的 W 和 H。
- 1–8 张修改结果。

点击“生成修改结果”后，控件立即隐藏。系统固定使用当前图片作为参考图，并使用编辑后的提示词和尺寸调用图片修改接口。

在修改控件外完整单击一次画布、其它节点或工具栏，修改控件会自动收起。拖动画布或节点不会收起；在控件内部编辑或点击当前图片时也不会意外关闭。

修改结果不会覆盖原图片：

- 新图片节点纵向排列在原图片右侧。
- 每张新图都会建立 `原图 → 新图` 有向连线，因为原图实际参与了这次生成。
- 新图保存本次修改提示词、模型、尺寸和原图版本快照。
- 如果原图之后被替换或发生变化，新结果会提示上游输入已更新。

没有填写图片服务 API Key 或提示词为空时不会开始生成，控件保持打开。接口请求失败时控件会重新打开，并保留用户刚才编辑的内容。

### 反推提示词

点击“反推提示词”后，新文本节点会包含一段普通可见文字，要求模型覆盖主体、构图、风格、光线、色彩、材质、镜头和氛围，并只输出可用于 AI 生图的提示词。

创建节点和连线后不会自动运行。用户可以先修改这段任务文字，再点击新文本节点的“生成”。上游图片会作为多模态图片输入传给兼容的文本模型。

## 视频节点

左侧工具栏可以创建空视频节点；“上传”可以读取本地视频并创建视频节点。上传后，节点使用浏览器原生播放器，可播放、暂停和拖动进度。

视频节点可以作为其他节点的直接上游，其文件 URL 和说明会被提取为视频类型输入。

当前视频节点虽然显示“生成”按钮并使用独立的视频服务配置，但实际请求仍然发送到该服务的 `POST /chat/completions`，返回内容作为文字结果显示。当前版本没有实现真实视频文件的创建、任务轮询或视频结果回填。

## 音频节点

左侧工具栏可以创建音频节点；“上传”可以读取本地音频并创建音频节点。音频可以作为其他节点的直接上游，文件 URL 和节点内容会被提取为音频类型输入。

音频节点支持从模型下拉框选择音频模型。点击“生成”后，系统读取直接连接的上游节点文字，调用所选服务的 `POST /audio/speech`，并把返回的音频文件回填到当前节点。生成完成后可直接使用节点内的播放按钮播放或暂停，通过长进度条拖动播放位置，并使用进度条右侧的扬声器按钮和音量条控制声音。

音频节点右上角“•••”菜单集中提供播放速度、从头播放和下载音频功能，不再使用浏览器原生播放器内部的“更多选项”菜单。

当前默认声音为 `alloy`，输出格式为 MP3。朗读内容最多 4096 字；超过限制时系统会在请求前提示。没有连接到音频节点的内容不会参与生成。

## AI 生成规则

### 上下文组成

点击某个节点的“生成”时，请求按以下顺序组织：

1. 全局系统提示词。
2. 当前画布提示词。
3. 当前节点所有直接、已启用的上游输入。
4. 当前节点自己的任务或隐藏任务。
5. 当前节点的用户输入。

每个上游输入都会保留输入顺序、节点名称、节点类型和内容边界，而不是简单把所有文字拼接在一起。

图片上游会额外作为 `image_url` 多模态内容传递。图片生成时，参考图片不会被重复转换成超长 Base64 文本塞入 prompt，而是作为独立图片文件发送。

### 直接上游规则

当前版本只读取一层直接上游。例如 `A → B → C`：

- 执行 B 时读取 A。
- 执行 C 时只读取 B，不会继续递归读取 A。

如果 B 的结果已经吸收了 A，C 可以通过 B 间接使用该信息。当前没有可配置的递归深度。

### 内容长度控制

为避免模型接口的 prompt 长度错误：

- 单个上游节点进入格式化上下文的文字最多 8000 字符。
- 所有格式化上游输入合计最多 20000 字符。
- 图片生成的最终 prompt 最多约 30000 字符，并为系统提示词和画面规格预留空间。

超出部分会被截断并附加说明。不能通过网页关闭这些保护限制。

### 输入变化与结果过期

每次生成会记录使用过的上游节点 ID、版本、当前提示词、模型和生成时间。

上游节点内容或标题变化后，如果当前版本与上次生成快照不同，下游节点会显示“上游输入已更新，建议重新生成”。系统不会自动重新生成，需要用户再次点击“生成”。

### 无密钥的本地演示

对应服务没有 API Key 时，网页不会发出网络请求，而是写入一段说明使用了多少直接上游输入的本地演示文字。该文字不代表模型真实输出。

## 配置中心

点击右上角“配置”打开配置中心。

### 模型服务

文本、图片、视频和音频各自可以添加 1–5 个模型渠道。首次使用时，每类服务都有一个“模型1”；点击“添加模型”可继续增加，达到 5 个后按钮不可用。每个模型渠道分别保存以下项目：

- 渠道名称，例如“模型1”或“文案模型”。
- 服务名称。
- API Base URL。
- API Key。
- 模型名称。
- 温度。
- 最大输出长度。

默认配置如下：

| 类型 | Base URL | 默认模型 | 默认温度 | 最大输出长度 |
|---|---|---:|---:|---:|
| 文本 | `https://api.openai.com/v1` | `gpt-5.5` | 1 | 4096 |
| 图片 | `https://api.openai.com/v1` | `gpt-image-2` | 1 | 4096 |
| 视频 | `https://api.openai.com/v1` | `sora-2` | 1 | 4096 |
| 音频 | `https://api.openai.com/v1` | `gpt-4o-mini-tts` | 1 | 4096 |

Base URL 会直接使用，仅移除末尾多余的 `/`，不会自动补上 `/v1`。配置兼容服务时，应填写该服务实际可用的 API 根地址。

“测试模型”会携带当前模型渠道的 Bearer API Key 请求 `GET <Base URL>/models`，并显示 HTTP 错误、读取到的模型数量，以及当前填写的模型名是否在返回列表中。此测试只验证 `/models`；部分不提供该端点的兼容服务即使其他接口可用，测试也可能失败。

每个模型渠道都可独立恢复默认，只重置当前渠道的服务配置和当前会话密钥。删除渠道时，原先选择该渠道的节点会回退到同类型剩余的第一个模型。

画布中每个文本、图片、视频和音频节点的“生成”按钮旁都有模型下拉框，只显示与该节点类型相同的模型渠道。节点会记住所选渠道；实际生成、图片批量子图和图片修改都使用该渠道的 URL、Key、模型名、温度和最大输出长度。修改节点的模型选择不会自动生成。

文本和视频的聊天请求使用 `max_completion_tokens`。名称以 `gpt-5` 或 `o` 加数字开头的模型不会发送 `temperature`；其他聊天模型会发送所配置的温度。当前图片和音频请求不会使用界面中的温度和最大输出长度；音频模型使用 `/audio/speech`，并发送 `model`、`input`、`voice` 和 `response_format`。

### 系统提示词

“全局系统提示词”定义所有模型请求使用的总体身份和行为。“当前画布提示词”补充当前画布的输出要求。

请求层级为：

`全局系统提示词 → 当前画布要求 → 直接上游输入 → 当前节点任务与用户输入`

板块底部“恢复默认”会同时重置全局系统提示词、当前画布提示词和工具确认策略。

### 存储与隐私

画布、视口和不含密钥的设置保存在浏览器 `localStorage`。各模型渠道的 API Key 分别保存在当前标签页会话的 `sessionStorage`，关闭会话后可能失效。

“清理全部本地数据”会清除本域名的全部 `localStorage`，并删除四种服务的会话密钥。该操作会影响已保存在当前浏览器中的画布和设置。

## 保存、导出与快捷键

### 项目菜单

点击左上角画布名称可打开项目菜单：

- “重命名画布”：打开名称输入框。按 Enter 或确认按钮保存，按 Esc 或取消按钮退出；名称最多 60 个字符。
- “我的画布”：展开当前浏览器中保存过的画布，显示名称、最近保存时间、节点数量、连线数量和“当前”标记。点击其它画布即可切换，切换前会先保存当前画布。
- “新建画布”：创建新的空画布并保存。
- “导出画布”：把当前画布导出为 ZIP 压缩包。压缩包根目录的 `canvas.json` 保存画布 ID、名称、节点、连线、视口和文件清单；`file/` 文件夹保存用户上传及生成后仍被节点引用的图片、视频和音频文件。JSON 中已成功打包的节点资源地址会改成对应的 `file/文件名`，不会修改网页中正在使用的画布数据。

远程生成文件受资源服务器跨域和有效期限制。浏览器能够读取时会一并打包；无法读取时，导出仍会完成，`canvas.json` 会保留原始 URL，并在 `skippedFiles` 中记录未打包原因。
- “保存到本地”：立即保存当前画布和设置。

新建画布会使用“未命名画布”；名称重复时自动追加序号。每个画布使用独立的本地存储记录，旧版本已经保存但没有入口的画布也会自动加入“我的画布”。

节点、连线或视口变化后，程序还会在约 900 毫秒无新变化时静默保存，并更新“我的画布”中的最近保存时间。

### 撤销与重做

画布左下角提供撤销和重做按钮。历史最多保存 50 个快照。创建、移动、缩放、连接、删除和部分编辑操作会写入快照；当前版本不保证每次文字键入都能单独撤销。

### 快捷键

| 快捷键 | 功能 |
|---|---|
| `Ctrl+Z` / `Cmd+Z` | 撤销 |
| `Ctrl+Shift+Z` / `Cmd+Shift+Z` | 重做 |
| `Ctrl+S` / `Cmd+S` | 保存到本地 |
| `Delete` / `Backspace` | 删除当前节点或连线 |
| `Esc` | 取消正在进行的连接 |
| `V` | 切换到选择模式 |
| `H` | 切换到移动画布模式 |
| `Ctrl+Enter` / `Cmd+Enter` | 完成 AI 文字结果编辑 |

光标位于普通 `input`、`textarea` 或 `select` 中时，全局快捷键不会触发。

## 推荐流程

### 文本生成图片

1. 新建文本节点，输入创意想法。
2. 点击文本节点“生成”，让文本模型整理成生图提示词。
3. 双击 AI 生成结果，按需人工修改。
4. 把确认后的文字复制回输入区，或另建文本节点保存最终版本。
5. 点击文本节点右上角“生图”。
6. 在新图片节点中设置自动或指定比例。
7. 点击图片节点“生成”。

注意：图片节点读取的是上游文本节点的输入内容，不会自动把该节点单独显示的 `resultText` 当作输入。因此需要用于生图的最终文字时，应确认它已经写入上游文本节点的内容区。

### 图片反推后再生图

1. 上传或生成一张图片。
2. 选中图片并点击“反推提示词”。
3. 在自动创建的文本节点中检查或修改反推任务。
4. 点击文本节点“生成”。
5. 把需要用于生图的最终文字整理到文本内容区。
6. 点击“生图”创建下游图片节点，再手动点击“生成”。

### 多资源输入

1. 把多个文本或图片节点连接到同一目标节点。
2. 点击各条连线，用 `↑` / `↓` 调整稳定输入顺序。
3. 在目标文本节点中键入 `@`，按需要插入已连接资源的显式引用。
4. 点击“生成”，并根据结果决定是否修改输入或停用某条连线。

## 当前限制

以下是当前代码中存在但尚未完整接入的界面或能力：

- 视频“生成”目前走聊天接口并返回文字，不会生成视频文件。
- 音频节点当前不能播放上传的音频，也没有音频生成入口。
- `@` 菜单只支持直接上游资源，不支持搜索未连接节点或递归上游。
- 图片、视频和音频的多模态格式是否被第三方 OpenAI 兼容服务接受，取决于该服务实际协议。
- 网页从浏览器直接请求模型服务，服务端必须允许当前网页来源的跨域请求。
- “画布偏好”中的“吸附到网格”和“自动保存间隔”目前仅保存选项，节点移动未执行吸附，实际自动保存仍固定为约 900 毫秒防抖。
- “工具确认策略”“保存操作记录”“保存生成记录”“允许发送画布上下文”和“匿名使用统计”目前会保存到本地，但没有接入生成或遥测控制逻辑。
- 服务名称只用于保存配置，不会改变请求协议；请求仍使用代码中固定的 OpenAI 兼容端点。
- 批量图片当前按张顺序调用接口；张数越多，等待时间和接口费用越高。单次最多设置 8 张。
- 上下文深度固定为直接上游一层，暂不能选择 2 层或整条链路。
