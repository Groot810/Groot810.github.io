# 任务中心发现

- 普通文本/图片/视频/音频生成统一由 `runNode()` 管理；已有图片的修改生成由 `runImageVariation()` 单独管理。
- 用户中断通过每个节点对应的 `AbortController` 进入 `AbortError` 分支，适合在该分支写入“已中断”。
- 任务摘要只需记录节点、媒体类型、渠道、模型、起止时间、状态和错误；重试时应重新读取节点当前上下文。
- 任务列表使用 localStorage 保存最近 100 条。页面刷新后无法继续原网络请求，因此遗留的“运行中”会安全恢复为“已中断”。
- 现有模板库和资产库都使用左侧浮动面板；任务中心复用同一布局并与二者互斥打开。
- 跨画布定位复用现有 `switchCanvas()`；画布数据应用后节点数组已立即可读，再用 `nextTick()` 等待界面更新即可计算居中视角。
- 历史任务若没有画布字段无法可靠反推来源，因此兼容显示“未知画布”，不进行可能错误的自动跳转。
# ecshopx-canvas 重命名

- 插件内部标识原本已经是 `ecshopx-canvas`；需要统一的是界面显示名、项目标题、npm 包名和 Skill 文案。
- 最终名称统一使用严格的小写连字符形式 `ecshopx-canvas`。
